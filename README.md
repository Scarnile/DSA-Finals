# Java Version

This uses Oracle OpenJDK 26.0.1. You can download this version through IntelliJ.

If you're still getting errors go to the View > Tool Windows > Maven and then "Sync all Maven Projects" or check if the
correct java version is installed in your device by typing `java --version` on the terminal and that java 26.0.1
appears.